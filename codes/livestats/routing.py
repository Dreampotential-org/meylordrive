# # livestats/routing.py
# from django.urls import re_path
# from .consumers import LiveStatsConsumer

# websocket_urlpatterns = [
#     re_path(r'^ws/live_stats/(?P<room_name>[^/]+)/$', LiveStatsConsumer.as_asgi()),
# ]
