services:
  mailserver:
    image: ghcr.io/docker-mailserver/docker-mailserver:latest
    container_name: mailserver
    # Provide the FQDN of your mail server here (Your DNS MX record should point to this value)
    hostname: dreampotential.org
    ports:
      - "25:25"
      - "465:465"
      - "587:587"
      - "143:143"
      - "993:993"
    volumes:
      - /data/maul/docker-data/dms/mail-data/:/var/mail/
      - /data/maul/docker-data/dms/mail-state/:/var/mail-state/
      - /data/maul/docker-data/dms/mail-logs/:/var/log/mail/
      - /data/maul/docker-data/dms/config/:/tmp/docker-mailserver/
      - /data/maul/etc/localtime:/etc/localtime:ro
    environment:
      - ENABLE_RSPAMD=1
      - ENABLE_CLAMAV=1
      - ENABLE_FAIL2BAN=1
    cap_add:
      - NET_ADMIN # For Fail2Ban to work
    restart: always
